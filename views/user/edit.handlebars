<script >
       
        var usuario = localStorage.getItem("usuario");
        
        if(usuario==null || usuario==undefined){
             console.log(usuario);
              window.location="/user/login"
             //window.location="/user/login"
        }
</script>
<h2>Editar - <a href="/user"> Administrador </a></h2>

<!-- NOTA: utilice helper para simplificar el código de esta sección 
agregar method-override 
-->

<form class="form" action="/user/{{user.id_administrador}}?_method=PUT" method="post">
  <div class="form-group">
    <label for="title"></label>
    <label>Id Usuario: </label><input type="text" class="form-control" id="id_administrador" name="id_administrador"  value="{{user.id_administrador}}" readonly="readonly"> 
    <label>Usuario: </label><input type="text" class="form-control" id="usuario" name="usuario"  value="{{user.usuario}}"> 
      <label>Contraseña: </label> <input type="text" class="form-control" id="password" name="password"  value="{{user.password}}">
     
        <label>Rol: </label> <select select class="form-control" id="rol"  value="{{user.rol}}" name="rol" type="text">
      <option>Visitante</option>
          <option>Administrador</option>
      
      </select>
  </div>
  <button type="submit" class="btn btn-default btn-success">Actualizar</button>
    <a href="/user" type="button" class="btn btn-default btn-warning">Atrás</a>
</form>

<br><br><br>
    <div class="row pt-md-5"></div>
